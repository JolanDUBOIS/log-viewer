{
  "name": "with-svelte",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:node": "concurrently \"node server-node/index.js ./local-tests/log.json --app --dev\" \"cross-env BACKEND_PORT=3001 vite\"",
    "dev:flask": "concurrently \"poetry run python -m server-flask.run --port 3002 --file ./local-tests/log.json\" \"cross-env BACKEND_PORT=3002 vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server-node/index.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "svelte": "^5.35.5",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "open": "^10.2.0"
  }
}
